dependencies:
  pre:
    - npm install -g firebase-tools

test:
  override:
    - echo OK

deployment:
  production:
    branch: master
    commands:
      - firebase deploy --token=$(FIREBASE_TOKEN)
      - echo Build
      - yarn build
      - echo Hosting
      - firebase deploy --only hosting
